

lets also research if there is a good way to set up unit tests using a popular testing library so they can be run via the command line without requiring the game to be running inside Roblox.  we will be able to develop much faster if you can easily run the tests without requiring human intervention
